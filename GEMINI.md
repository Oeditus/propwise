# Workspace Rules

- **Direct File Modifications**: Always execute file modifications directly without asking the user for confirmation beforehand. The user relies on version control (git) to manage and review code changes.
